# PORT CHECK REPORT
**时间：2026-06-02 约 22:00**

---

## 当前监听端口汇总

| 端口 | 进程 | PID | 命令 | 说明 |
|:----:|------|:---:|------|------|
| 3001 | node | 23881 | `next-server (v16.2.6)` | fitness-crm 开发服务器 |
| 10794 | falemonht | 1423 | 飞书守护进程 | 飞书客户端本地服务 |
| 10795 | falemonlf | 63863 | 飞书守护进程 | 飞书客户端本地服务 |
| 14013-14023 | WeChat | 45728 | 微信客户端 | 微信本地端口 |
| 52154 | rapportd | 476 | macOS 系统服务 | Apple 设备间通信 |
| 57843 | wpsclouds | 61862 | WPS 云服务 | WPS 后台 |
| 55757-55774 | QQMusic | 64065 | QQ 音乐 | 音乐客户端 |
| 15721 | cc-switch | 99835 | CCX 相关 | CCX 端口切换服务 |

---

## 1. 当前有哪些端口在运行

共 **9 个应用/服务** 监听本地端口，其中 **唯一一个 Node.js 开发服务器** 在 **3001** 端口。

## 2. 3001 对应哪个进程

| 属性 | 值 |
|------|-----|
| PID | **23881** |
| 父 PID | **23874** (`next dev` 命令) |
| 命令 | `next-server (v16.2.6)` |
| 工作目录 | `/Users/qiao/Documents/徕舞工作/fitness-crm` |
| 协议 | IPv6 (也接受 IPv4 连接) |

## 3. 4000 对应哪个进程

**4000 端口没有任何进程监听。** 处于空闲状态。

之前曾临时在 4000 端口启动过 Next.js，但该进程已被杀死（`kill -9` 后未重启）。

## 4. 两个端口是不是同一个项目

**不是。** 只有 3001 端口在运行 fitness-crm。4000 端口空闲。

## 5. 为什么会同时存在

**不存在同时存在的情况。** 当前只有 3001 端口在运行。

历史上 4000 曾短暂出现的原因是：沙箱内 `npm run dev` 无法监听端口时，用了 `-H 127.0.0.1 -p 4000` 作为临时方案。该进程已在诊断过程中被清理。

---

## 结论

**fitness-crm 仅在 `http://localhost:3001` 运行，PID 23881，Next.js 16.2.6。**

无需清理任何进程。
