# LAUNCH_BLOCKERS.md

> 上线阻塞项清单
> 扫描时间：2026-06-04
> 范围：CRM + Portal + 品牌官网

---

## P0 — 必须修复（阻塞真实试用）

### B1. CRM Dashboard 使用旧颜色体系（低影响但整体系）

- **文件：** `src/components/dashboard/StatsCards.tsx`, `SidePanel.tsx`, `MemberList.tsx`
- **问题：** 大量硬编码 `#9E8E7E`（旧辅助色）、`font-serif`（已禁止字体）、`font-bold`（品牌规范禁止）
- **影响：** CRM 后台视觉与品牌规范不一致，但不影响功能
- **严重度降级原因：** CRM 是内部工具，非客户展示面

**状态：** 降级为 P2（不阻塞试用）

### B2. 示例数据中「蜕变」禁用词未清理

- **文件：** `src/app/api/ai-report-sample/route.ts` 返回的示例数据
- **问题：** 示例报告中包含「完成了从产后恢复到重获力量的蜕变」——「蜕变」是品牌禁止词
- **影响：** 如果真实会员收到含「蜕变」的报告，违反品牌规范
- **修复：** 需要清理示例种子数据

**状态：P0 必须修**（这是对外展示的）

---

## P1 — 建议修复

### B3. Cases API 图片路径引用不存在

- **文件：** `src/lib/cases/cases.ts`
- **问题：** `photoPath: "/cases/case-1.webp"` 指向的 `/public/cases/` 目录不存在
- **影响：** API 返回了 `photoPath` 但前端如果不渲染图片就没事（目前品牌页零图片策略）
- **风险：** 如果有任何前端组件尝试渲染这些图片，会显示破损图

**状态：P1 建议修**（尽早删除 photoPath 引用，避免别人误用）

### B4. 样本数据需要与品牌规范对齐

- **文件：** 种子数据库中的示例报告内容
- **问题：** 示例报告中可能包含其他禁止词（蜕变、突破、逆袭等）
- **影响：** 如果演示时被看到，影响品牌印象

**状态：P1 建议修**

---

## P2 — 可延期

### B5. CRM Dashboard 视觉统一

- **涉及文件：** ~5 个 dashboard 组件
- **工作量：** 约 1-2 小时
- **延期原因：** 不影响功能，CRM 是内部工具

### B6. API 路由路径 `/api/ai/` 保留

- **现状：** 内部 API 使用 `/api/ai/period-report` 等路径
- **影响：** 不展示给用户，纯代码层面
- **决定：** 本次不改（避免大面积路由重构），后续 Sprint 逐步迁移到 `/api/growth/`

### B7. DESIGN_TOKEN.ts 中旧字体规范未清理

- **文件：** `src/lib/design/DESIGN_TOKEN.ts`
- **问题：** TYPOGRAPHY 对象仍引用 `font-serif`、`font-bold`、旧色 `#9E8E7E`
- **影响：** 全站组件已不使用，但作为参考可能误导新开发者
- **修复：** 更新 TYPOGRAPHY 对象与当前实际使用的规范一致

### B8. Portal share 页面有英文残留

- **文件：** `src/app/portal/share/page.tsx`
- **问题：** 含有 `"Laiwu Journey"` 英文字符串
- **影响：** 极小（仅在分享页使用）

---

## 修复优先级总结

| ID | 描述 | 优先级 | 阻塞试用? |
|----|------|--------|-----------|
| B2 | 示例数据含「蜕变」禁止词 | **P0** | 否（但必须修） |
| B3 | Cases 图片路径不存在 | P1 | 否 |
| B4 | 种子数据品牌对齐 | P1 | 否 |
| B5 | CRM 视觉统一 | P2 | 否 |
| B6 | API 路由重命名 | P2 | 否 |
| B7 | DESIGN_TOKEN 清理 | P2 | 否 |
| B8 | 英文残留 | P2 | 否 |

## 结论

**当前状态：可以开始真实试用。**

唯一 P0 项（B2）是示例数据文案对齐，不影响系统功能。所有 API 正常响应，所有页面正常加载，Build 通过，TypeScript 零错误。
